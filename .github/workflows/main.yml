name: test-1
run-name: ${{ github.actor }} is learning GitHub Actions
on: push
jobs:
  run-on: self-hosted
  Install:
    steps:
      - name: npm install
        run: npm install
  Deploy:
    steps:
      - name: deploy
        run: docker run
      
      
      
      
          
