name: test-1
run-name: ${{ github.actor }} is learning GitHub Actions
on: push
jobs:
  Install:
    run-on: self-hosted
    steps:
      - name: npm install
        run: npm install
  Deploy:
    steps:
      - name: deploy
        run: docker run
      
      
      
      
          
