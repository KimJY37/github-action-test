name: test-1
run-name: ${{ github.actor }} is learning GitHub Actions
on: push
jobs:
  Install:
    steps:
      - name: npm install
        run: npm install
jobs:
  Deploy:
    steps:
      - name: deploy
        run: docker run
      
      
      
      
          
